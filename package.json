{
  "name": "ai-prompt-feishu-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ai-prompt-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}
