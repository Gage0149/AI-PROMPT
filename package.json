{
  "name": "ai-prompt-gallery-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 migrations apply ai-prompt-gallery-db --local",
    "db:init:remote": "wrangler d1 migrations apply ai-prompt-gallery-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
